# Länggass-Tee fixtures

The adapter consumes the public Shopware Store API. Keep anonymised API listing-page responses or
focused product dictionaries here whenever parser behavior is added or repaired. Fixtures must be
committed with the corresponding regression test.
