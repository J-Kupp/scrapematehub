# Walker fixtures

`product.html` is a reduced, non-secret reference fixture based on the public Walker product page structure.

The public shop currently returns `User-agent: *` and `Disallow: *` in `robots.txt`. Automated live crawling must remain disabled until Walker grants explicit permission. Public prices are also hidden without a Walker shop login.
